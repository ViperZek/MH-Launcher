# Mise à jour du launcher

- `update.json` indique la dernière version.
- `updates/<version>/` contient les fichiers modifiés.
- Chaque fichier doit être ajouté dans `files` avec son URL GitHub et son empreinte SHA-256.
